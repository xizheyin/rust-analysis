// 数组初始化
void main() {
    int arr[100];
    for (int i = 0; i < 100; i++) {
        arr[i] = i * 2;  // 初始化数组元素
    }

    // 数组初始化完成
    for (int i = 0; i < 100; i++) {
        printf("%d", arr[i]);
    }
}
